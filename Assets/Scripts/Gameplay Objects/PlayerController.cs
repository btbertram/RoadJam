using System.Collections;
using System.Collections.Generic;
using UnityEngine;

//Temp empty class, to probably be used for refactoring later
//ToDo: Seperate the player's controls from the player cursor (which should just be the
//visual representation of the player only)
public class PlayerController : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
